import { PipeStringPipe } from './pipe-string.pipe';

describe('PipeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
